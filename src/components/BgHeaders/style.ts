import styled from 'styled-components'
import { colors } from '../../styles'
import vetorBG from '../../images/Vector-gf.svg'

export const bgHeader = styled.header`
  background-color: ${colors.pinkMedium};
  background-image: url(${vetorBG});
  align-items: center;
`
